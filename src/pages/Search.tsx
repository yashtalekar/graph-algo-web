import React from "react"

const Search = () => {
  return (
    <div>
      <div>Welcome to Search Algorithms</div>
      <div>
        There are a variety of search algorithms for graphs. They have different uses as well, some search if a goal
        graph node exists or not, like BFS and DFS, while some also find the shortest path between the start node and
        the goal node like Dijkstra's Algorithm.
      </div>
    </div>
  )
}

export default Search
